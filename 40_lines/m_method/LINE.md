# LINE — Method (m)

Glifo/color heredados de la firma congelada de 000: línea · sangre `#C73A4A`.

**Qué es:** la línea que produce y valida el método (diseño → contratos →
auditoría → congelar → slice → refutación) y sus skills.

**Estado heredado (evidencia, 2026-07-20):**
- Método de 12 etapas + DESTILADO: estrato externo `development-system/`
  (fuente) y copia divergente en `psicoandino/legacy/` (ver ARCHIVE.md).
- Skills instaladas y en uso: arquitecto, auditor, contracts, find-atom,
  next-move, route, separate-systems (`~/.claude/skills/`), concilio (app).
- Validación del método por su propia regla: **1 de 3 proyectos** (Radiomisor
  retroactivo ✓; research-system en curso; tercero pendiente). El método no
  puede declararse validado todavía.

**Pregunta abierta de la línea:** ¿el método sobrevive su segunda corrida
(research-system) sin Fable como muleta — la "PRIMERA corrida sin Fable" que
el estrato externo ya había declarado como objetivo?

**Cierres Clase O posibles para esta línea:** un tercero (tesista, colega)
ejecuta el método con los runbooks y produce su propio slice — eso sería
también la primera evidencia de R-004.
